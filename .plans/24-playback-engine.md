# #24 Playback decision engine and timeline derivation

## Summary

Add a pure playback module under `apps/server/src/playback`. It accepts normalized client and probe data. It returns stream decisions, a play method, segment boundaries and adaptive variants. Later API and transcoder slices consume these values.

Read `CONTEXT.md`, `docs/spec/playback.md`, `docs/spec/transcoding.md` and ADR 0009 before changing the rules. The playback spec wins over older research recommendations. Keep database access, ffmpeg, profile translation, playlists and sessions outside this slice.

## Acceptance criteria

- [x] Table-driven tests cover every row of the per-stream table and every play-method outcome in the spec.
- [x] Dolby Vision profiles 7 and 8 resolve to the HDR10 base layer on a client without DV; profile 5 resolves to the CPU tone map.
- [x] TrueHD and DTS-HD pass only on direct play and follow the audio rule in HLS.
- [x] Timeline derivation covers a regular 2 s GOP and irregular keyframes. Alignment accepts a superset and rejects a missing boundary.
- [x] The effective cap is the minimum of the three inputs. No policy cap applies on the LAN.

## TODOs

- [x] Define the client profile, caps, ladder and backend capabilities.
  - Use readonly plain data. Client video entries carry codec, accepted profiles, maximum level and dimensions, in preference order.
  - Apply the minimum policy cap off LAN. Keep the client's decoder bitrate limit separate from the LAN exemption.
  - Add the five named ladder rates and CPU capabilities. Select only an asserted backend, in QSV, VAAPI, NVENC, Vulkan, CPU order.
  - Validate cap permutations, missing caps, LAN, all ladder thresholds, below-minimum refusal, CPU fallback and backend preference. Run focused Bun tests and the server typecheck.
  - Done: playback/policy.ts, playback/policy.test.ts. Red run failed on the missing module before implementation; `bun test apps/server/src/playback/policy.test.ts` passed 43 tests and 45 assertions: all six cap permutations, lone and tied caps, LAN bypass, literal ladder, all thresholds plus below-minimum undefined, CPU codec and tone-map assertions, backend preference and forceCpu. `bun run --cwd apps/server check` and `bunx --no-install biome check apps/server/src/playback` clean. Evidence: .devin/todo1-red.log, .devin/todo1-green.log, .devin/todo1-check.log, .devin/todo1-lint.log.
- [x] Implement per-stream decisions and the resulting play method.
  - Test the public decision function with table rows for every video constraint, audio branch and subtitle branch.
  - Decide direct play first. Re-evaluate lossless audio for HLS when any stream or container prevents direct play.
  - Preserve HDR10 base layers for DV 7 and 8. Force the CPU tone map for unsupported DV 5.
  - Copy supported text. Convert unsupported text to sidecar WebVTT without video re-encoding. Burn unsupported bitmap subtitles.
  - Select a supported encoder and ladder rung only when video needs re-encoding. Preserve aspect ratio and never upscale.
  - Validate all three play methods, DV cases, TrueHD and DTS-HD in direct play and HLS, plus unavailable output paths. Run focused Bun tests and the server typecheck.
  - Done: playback/decisions.ts, playback/decisions.test.ts. Red run against a naive all-copy stub failed 55 of 72 tests before implementation; `bun test apps/server/src/playback/decisions.test.ts` then passed 72 tests and 114 assertions: every video constraint row and boundary, all three play methods, text convert versus bitmap burn, DV 7/8 base-layer copy and strip flag, DV 5 forced CPU tone map, TrueHD and DTS-HD direct play versus HLS fallback, decoder cap on LAN, rung selection, scaling and no-upscale, throws for missing rung, backend and AAC fallback, plus input immutability. `bun run --cwd apps/server check` and `bunx --no-install biome check apps/server/src/playback` clean. Evidence: .devin/todo2-red.log, .devin/todo2-green.log, .devin/todo2-check.log, .devin/todo2-lint.log.
- [x] Derive segment timelines and check foreign Version alignment.
  - Derive boundaries nearest to each previous boundary plus four seconds. Ties choose the earlier keyframe.
  - Include zero and the duration endpoint. Require keyframes at segment starts, not at the terminal duration.
  - Reject malformed keyframe inputs rather than inventing cut points. Use exact timestamps for alignment.
  - Validate regular and irregular GOPs, ties, short files, trailing segments, equal and superset keyframes, missing boundaries and duration mismatch. Run focused Bun tests and the server typecheck.
  - Done: playback/timeline.ts, playback/timeline.test.ts. Red run against a shaped stub failed 33 of 44 tests; `bun test apps/server/src/playback/timeline.test.ts` then passed 44 tests and 44 assertions: regular and irregular GOPs, trailing segments, ties choosing the earlier keyframe, short and sparse files, all malformed-input throws, exact and superset alignment, missing boundaries, duration mismatch, near-miss timestamps, malformed boundaries and keyframes returning false, derivation-alignment round trips and input immutability. `bun run --cwd apps/server check` and `bunx --no-install biome check apps/server/src/playback` clean. Evidence: .devin/todo3-red.log, .devin/todo3-green.log, .devin/todo3-check.log, .devin/todo3-lint.log.
- [x] Select adaptive groups from compatible aligned Versions.
  - Filter to the requested Item and timeline, aligned imported Versions and complete Stored Versions.
  - Reuse the video pass rule. Choose the first available client codec family and sort variants by increasing bitrate.
  - Prefer playable existing Versions over a live video transcode, including when only a lower-ranked family passes.
  - Return one live source for later session planning only when no existing variant passes. Do not offer incomplete Stored Versions as fallback sources.
  - Validate mixed families, cuts, alignment, completeness, caps, sort order, stored preference, live fallback and unchanged inputs. Run focused Bun tests and the server typecheck.
  - Done: playback/adaptive.ts, playback/adaptive.test.ts. Red run against a null-result stub failed 25 of 28 tests; `bun test apps/server/src/playback/adaptive.test.ts` then passed 28 tests and 70 assertions: family preference, ascending bitrate sort, lower-ranked family over live encode, unsupported codec exclusion, all seven source and alignment exclusions, imported superset acceptance, stored flags without keyframes, cap filtering on WAN and LAN with the decoder limit still applying, live source preference for the highest bitrate eligible source, null results for no eligible sources, no duplicate variants and unchanged inputs. `bun run --cwd apps/server check` and `bunx --no-install biome check apps/server/src/playback` clean. Evidence: .devin/todo4-red.log, .devin/todo4-green.log, .devin/todo4-check.log, .devin/todo4-lint.log.
- [x] Validate the complete slice and record the results.
  - Review every source and test against the spec and acceptance criteria. Keep imports inside the pure module.
  - Run from the root: `bun install --frozen-lockfile`, `bun run lint`, `bun run check`, `bun run build`, `bun test`.
  - Record real results below, including database skips. Commit this TODO before rebasing, pushing and filing the PR.
  - Done: `bun install --frozen-lockfile` checked 91 installs, no changes; `bun run lint` clean across 67 files; `bun run check` 5/5 turbo tasks; `bun run build` 4/4 turbo tasks, server bundle 235.1 KB; `bun test` 206 pass, 62 skip, 0 fail across 16 files with database tests skipped for missing DATABASE_URL. All exit codes 0. Evidence: .devin/final-install.log, .devin/final-lint.log, .devin/final-check.log, .devin/final-build.log, .devin/final-test.log.

## Notes

- Worktree `/home/mia/mia-cx/pendia/.worktrees/playback`, branch `feat/24-playback-engine`, starting at `f8c6ac1`. Issue #20 is closed.
- This run is unattended. Tests use exported pure functions as the agreed seam. Each TODO gets its own reviewed implementation and commit with `Refs #24`.
- Read both playback research documents, the Version/Stream/timeline schema, colocated test conventions and the server README.
- The profile is Pendia's normalized contract, not Jellyfin's DeviceProfile wire format. Translation and admin overrides belong to later slices.
- Bitrates use bits per second. Null or absent caps mean no limit. LAN removes policy caps, not decoder limits. Equality fits a cap.
- The spec does not assign rung dimensions. Use bounding boxes of 3840x2160, 1920x1080, 1920x1080, 1280x720 and 854x480 for 20, 10, 6, 3 and 1.5 Mbit/s. Scale down within the source and client bounds, preserving aspect ratio with even output dimensions.
- The spec does not rank codecs. The normalized profile orders video capabilities from best to least preferred. Existing compatible Versions beat live re-encoding in another family.
- Inputs describe the selected video, audio and subtitle Streams, not every unselected Stream in a File. Probe normalization supplies known dimensions, bitrate and channels. Optional profile and level constraints require known matching source values.
- Unsupported output combinations throw rather than return an unplayable plan. A cap below 1.5 Mbit/s has no ladder rung. AAC stereo is the spec's baseline audio fallback.
- Adaptive selection returns at most one live source for later session planning. It does not plan the fallback's encoding, audio or subtitles; a later caller decides those for the session. Stored variants require native HDR support because ADR 0009 serves their segments unchanged. Imported variants retain remux eligibility through the video pass rule.
- CPU capability assertions describe the MVP contract, not a trial executed by this module. Hardware entries arrive from a later startup trial. Every DV 5 video re-encode selects CPU tone mapping, even on a DV-capable client or when hardware advertises tone mapping. Compatible DV 5 copies still pass.
- Timeline boundaries include the duration so callers can derive the final segment length. The endpoint is not a segment start and need not be a keyframe. Foreign alignment also requires the same duration. No timestamp tolerance is invented.
- Push after the pre-PR rebase onto `origin/main`, so no force-push is needed. Never commit `.devin`, touch another branch or merge the PR.
- Review existing CodeRabbit, Codex and Pullfrog findings with the trigger test. Finish only with passing CI, a clean Pullfrog verdict on the head, mergeability and no unresolved threads.
- Final gate results (recorded in .devin/final-*.log): `bun install --frozen-lockfile` no changes; `bun run lint` clean; `bun run check` 5/5 turbo tasks; `bun run build` 4/4 turbo tasks; `bun test` 206 pass / 62 skip / 0 fail with database tests skipped locally and one skip message; CI supplies its own disposable database.
- PR #52 review fix (Pullfrog thread PRRT_kwDOUQpMs86hzYvC, comment 3997505134): DV 7/8 base-layer copies carried stripDolbyVision through direct play, but range requests cannot rewrite the bitstream. Smallest fix: the direct-play gate in decidePlayback now excludes strip copies, so DV 7/8 remuxes, or transcodes when HLS forces lossless audio conversion; videoPasses is unchanged because the HDR10 base layer still plays without re-encoding and can join adaptive groups. The two existing DV 7/8 expectations encoded the defect and were corrected from direct-play to remux; DV fixtures moved to HEVC main10 for realism, and a regression table covers lossless audio under DV stripping. Red run failed exactly the 4 corrected/new assertions; `bun test apps/server/src/playback/decisions.test.ts` then passed 74 tests and 120 assertions; `bun run lint`, `bun run check` and `bun run build` clean. Evidence: .devin/review1-red.log, .devin/review1-test.log, .devin/review1-lint.log, .devin/review1-check.log, .devin/review1-build.log.
- Codex follow-up, comment 3997531608: a store job can remux a DV source rung (playback spec, Stored Versions), and ADR 0009 serves its segments unchanged. The adaptive filter incorrectly accepted unstripped DV 7/8 for HDR10-only clients. Stored variants now require native HDR support; rejected complete Versions remain eligible as live sources. Two regression rows failed before the fix. All 32 adaptive tests pass, including native DV and already-stripped HDR10 controls.
- Codex follow-up, comment 3997531611: the bitrate, resolution and bitmap-subtitle rules force video re-encoding even on DV-capable clients. DV 5 then incorrectly claimed DV output without a tone map. Every DV 5 re-encode now uses CPU tone mapping to SDR. Three regression rows failed before the fix; all 77 decision tests pass, including unchanged native DV 5 copying.
- Follow-up validation: `bun install --frozen-lockfile` checked 91 installs with no changes; `bun run lint` passed across 67 files; `bun run check` passed 5/5 tasks; `bun run build` passed 4/4 tasks; `bun test` passed 215 tests with 62 documented database skips and zero failures. Evidence: .devin/review2-adaptive-{red,green}.log, .devin/review2-decisions-{red,green}.log and .devin/review2-{install,lint,check,build,test}.log.
- Later Codex review, comment 3997585984: a cap forces DV 7/8 video re-encoding even when the client supports DV. The encoder table asserts no DV metadata preservation, so these re-encodes now use the HDR10 base layer. Clients without HDR10 receive SDR through HDR10 tone mapping. Four regression rows failed before the fix; compatible native DV copies still pass.
- Later Codex review, comment 3997585985: an unaccepted container or a Stream transcode requires HLS. The old decision kept supported SRT or ASS unchanged, contrary to the WebVTT rendition rule. HLS now re-evaluates text subtitles for sidecar WebVTT conversion. The rejected-container expectation encoded the defect and was corrected. That assertion and three new regression rows failed before the fix. Supported WebVTT copies and direct-play subtitle behavior remain unchanged.
- Later review validation: all 204 playback tests pass; server typecheck, playback Biome check and server build pass. Evidence: .devin/review3-dv-{red,green}.log, .devin/review3-hls-{red,green}.log and .devin/review3-{test,check,lint,build}.log. The preceding head passed all 277 repository CI tests with disposable Postgres; CI re-runs the full gate after this push.
- HDR output review, Pullfrog comment 3997676373 and overlapping Codex comment 3997682853: the existing client profile order can select H264 High or HEVC Main while preserving HDR10. Output selection now uses HEVC Main10 or AV1 Main for HDR when accepted. Other output choices tone-map to SDR, preserving client codec order. Backend selection checks the resulting per-candidate tone map. Four regression rows failed before the fix; the AV1 control stayed green.
- Codex comment 3997682863: Stream metadata permits a missing or other DV profile. Such video previously claimed DV preservation when forced to re-encode. The output rule now tone-maps any remaining DV input instead. Null and profile 9 cases pass; temporarily removing the new DV-preservation guard makes both fail. That proof is labeled as a guard-reversion check rather than a pre-edit baseline.
- HDR output validation: the scoped gate passed 209 playback tests, server typecheck, playback Biome check and server build. After adding the two unknown-DV regressions, all 92 decision tests, server typecheck and touched-file Biome check pass. Evidence: .devin/review4-{red,green,test,check,lint,build}.log and .devin/review4-unknown-dv-{red,green,check,lint}.log. The preceding head passed all 285 repository CI tests with disposable Postgres.
- Pullfrog comment 3997733739 was rejected and withdrawn after re-review. Its proposed trigger was a codec-only startup trial misclassifying hardware, but no trial producer exists in this slice. The capability table is trusted input matching the spec; future trial code must validate the outputs it asserts. The table remains unchanged.
- Codex comment 3997737756: separate HEVC Main and Main10 entries caused an early SDR fallback despite an available HDR profile. Output selection now keeps first-seen codec-family order and tries HDR-capable entries within each family first. Entries retain their own level and dimension bounds. Two regression rows failed before the fix; all 213 playback tests, server typecheck, playback Biome check and server build pass. Evidence: .devin/review5-{baseline,red,green,test,check,lint,build}.log. The preceding head passed all 292 repository CI tests with disposable Postgres.
- Codex comment 3997764054: a level-limited client could receive a selected rung whose bitrate or scaled dimensions exceeded that level. Output selection now skips such candidates, or throws when none fits. This keeps the existing rung policy rather than inventing a lower-rung preference. Conservative baseline/main-tier bounds cover H264, HEVC and AV1 using their native level identifiers. The bounds end once all fixed-ladder sizes and rates fit. Sources: https://raw.githubusercontent.com/FFmpeg/FFmpeg/n7.1/libavcodec/h264_levels.c, https://raw.githubusercontent.com/FFmpeg/FFmpeg/n7.1/libavcodec/h265_profile_level.c and https://raw.githubusercontent.com/FFmpeg/FFmpeg/n7.1/libavcodec/av1_levels.c. Only numeric standard limits are encoded; the module gains no ffmpeg integration. Four output regressions failed before the fix. All 226 playback tests, server typecheck, playback Biome check and server build pass. Evidence: .devin/review6-{baseline,red,green,test,check,lint,build}.log.
- Codex comment 3997764057 was rejected: the per-stream contract copies supported PGS/VobSub and does not require embedding them in fMP4. The cited research records native external bitmap subtitle support. A future delivery layer must honor that support rather than force every supported bitmap to burn.
- Codex comment 3997764061 was rejected: SDR is the baseline, while the HDR list names optional renderer flavours. No SDR-incapable supported client was identified. An empty HDR list remains an SDR client, not an unusable client.
- Pullfrog comment 3997839328: level limits also constrain macroblocks or luma samples per second. Every video transcode now returns `maxFrameRate`, computed from the selected output dimensions and the client's level. This is a ceiling for the future transcoder to enforce, not a request to upsample slower input. Null means no client-level ceiling; copies are unchanged. The numeric tables now include the higher picture-rate levels from the same standard references. Three regressions failed before the fix. Exact transcode expectations were extended to assert the previously omitted constraint. All 236 playback tests, server typecheck, playback Biome check and server build pass. Evidence: .devin/review7-{red,green,test,check,lint,build}.log. CI on the preceding rebased head passed all 331 repository tests with disposable Postgres.
- Codex comment 3997870724: AV1 also caps frame headers per second independently of picture size. The frame-rate ceiling now takes the minimum of its sample-rate limit and MaxHeaderRate: 150 for AV1 levels 2.0 through 3.1, 300 for the listed higher levels. Both small-frame regressions failed before the fix. All 238 playback tests, server typecheck and playback Biome check pass. Evidence: .devin/review8-{red,test,check,lint}.log. CI on the preceding head passed all 341 repository tests.
