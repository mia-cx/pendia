# #26 Shows medium with next up

## Summary

Add the shows medium beside movies. It recognizes Sonarr-style show and season layouts, writes Show, Season and Episode Items, and keeps Versions only on Episodes. Split episode files become ordered Files on one Version. Multi-episode files become one Episode with an inclusive range. The medium joins the video Home shelves, adds next up per user, and registers routes for all three kinds.

## Acceptance criteria

- [x] A fixture shows tree yields Show, Season and Episode Items with the closure table filled.
- [x] A split episode is one Version with two Files; a multi-episode file is one Episode carrying its range.
- [x] Next up returns the first unwatched episode after the last watched one per show, and nothing for a finished show.
- [x] Scan rules have tests on season folders, specials and both multi-file cases.

## TODOs

- [x] 1. Add the shows Medium definition and Sonarr path grouping.
  - Register Show, Season and Episode against their existing extension tables. Only Episode has Versions.
  - Use the top-level show folder as the canonical folder. Accept `Season N` and `Specials`, with Specials as season zero. Parse `SxxEyy`, `SxxEyy-Ezz` and `SxxEyyEzz` episode forms.
  - Group `partN`, `ptN` and `cdN` filename suffixes into one Version with ordered paths. Keep one multi-episode path as one Episode carrying its start and end numbers. Apply the movies medium's video, extras and Pendia-store rules.
  - Validation: `bun test apps/server/src/mediums/shows.test.ts`, `bun run --cwd apps/server check`, and `bun run --cwd apps/server build` pass. Tests cover season folders, Specials, ranges, split parts, extras, unsupported files and unsafe paths.
- [x] 2. Persist one canonical show tree through the scan pipeline.
  - Scan a canonical show folder recursively. Probe accepted files through the existing cache.
  - Insert or reuse the Show, its Seasons and Episodes through `insertItem`, preserving closure rows and curated Item metadata.
  - Write one imported Version per grouped episode version. Aggregate split-file bytes and duration, attach ordered Files and their Streams, and keep repeat scans identity-stable.
  - Validation: `DATABASE_URL=... bun test apps/server/src/libraries/scan.test.ts` passes against disposable Postgres and real ffmpeg fixtures. It asserts the Item tree, extension rows, closure rows, split and ranged episodes, Version ownership, File order, Streams, probe reuse and no container Versions.
- [x] 3. Enable shows libraries and queued show scans.
  - Permit `shows` in library creation and scan requests.
  - Dispatch root and directory jobs by library medium. A shows root job fans out once per canonical show folder and a directory job runs the shows scanner before publishing `library.changed`.
  - Keep the existing per-library concurrency key and movie behavior unchanged.
  - Validation: `DATABASE_URL=... bun test apps/server/src/libraries/service.test.ts apps/server/src/libraries/jobs.test.ts apps/server/src/api/libraries.test.ts` passes. Tests cover shows creation, root fan-out, queued scanning and the existing movie path.
- [x] 4. Add the next up shelf and registered show screens.
  - Add a database-bound shows medium factory so its shelf callback can query Progress without changing the Medium contract.
  - Return one candidate for each started show. The candidate is the first non-completed Episode after that show's highest completed Episode in season and episode order. Omit shows with no completed progress and shows with no later Episode.
  - Sort shows by the selected last Episode's `playedAt`, newest first, then deterministically. Join continue watching and recently added. Register `/shows/:id`, `/shows/:showId/seasons/:id` and `/shows/:showId/seasons/:seasonId/episodes/:id`.
  - Validation: `DATABASE_URL=... bun test apps/server/src/mediums/shows.test.ts` passes. Tests cover independent users, a partially watched candidate, multiple shows, season boundaries, Specials and a finished show.
- [x] 5. Complete acceptance coverage and run the repository gate.
  - Review every issue criterion against a permanent test and update the existing server README for shows scanning.
  - Run from the repository root: `bun install --frozen-lockfile`, `bun run lint`, `bun run check`, `bun run build`, `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55426/pendia bun test`, and `env -u DATABASE_URL bun test`.
  - Confirm the existing CI-only missing-DATABASE_URL failure and remove `pendia-test-pg-26` when review validation ends.
  - Validation: every command has its real result recorded below, local omission prints one skip message, and no `.devin` file is committed.

## Notes

- TODO1 done: `apps/server/src/mediums/shows.ts` (`showsMedium` satisfies Medium over the existing shows/seasons/episodes tables; `groupShowPaths` returns ShowPathGroup/SeasonPathGroup/EpisodePathGroup/ShowVersionPathGroup) plus colocated `shows.test.ts`. Results: `bun test apps/server/src/mediums/shows.test.ts` 19 pass 0 fail, `bun run --cwd apps/server check` clean, `bun run --cwd apps/server build` bundled 602 modules, `bunx biome check` on the three files clean, `git diff --check` clean. Decisions: Season canonicalFolder is `<show>/<season-folder>`; season titles default to `Specials`/`Season N`; episodes merge on episodeNumber keeping the widest end; a split Version key is the marker-stripped stem while every non-split path is its own Version; the literal top-level `extras` folder stays reserved and `isVideoExtra` runs on the path after the show folder.
- TODO2 done: `scanShowDirectory` in `libraries/scan.ts` walks the show folder recursively, selects the matching `groupShowPaths` group, probes each unique member through the cache, then in one transaction locks the library, re-stats members, reuses or inserts Show/Season/Episode via `insertItem` (episode end number updated on mismatch), and writes one imported Version per grouped version with summed bytes/duration, ordered Files and upserted Streams. The duplicated stream upsert became the private `upsertFileStreams` helper; movie path unchanged. Red run: `Export named 'scanShowDirectory' not found`. Green: `DATABASE_URL=... bun test apps/server/src/libraries/scan.test.ts` 6 pass (5 movie + `writes a Show tree with split and ranged Episode Versions`, 104 expects), `check`, `build`, `bunx biome check` on the three files, `git diff --check` all clean. `pendia-test-pg-26` started and left running on 127.0.0.1:55426.
- TODO3 done: `api/schema.ts` LibraryInput accepts `"movies" | "shows"`; `service.ts` dropped both movies-only rejections; `jobs.ts` picks `moviesMedium.scan`/`showsMedium.scan` and `groupMoviePaths`/`groupShowPaths` by `library.medium` and runs `scanDirectory`/`scanShowDirectory` per directory job. Red run: 5 fails (`scanShowDirectory` absent was not one; failures were service INVALID_INPUT on shows create and scan, jobs INVALID_INPUT on the shows root job, API 400 on shows create, ORPC BAD_REQUEST on the typed client). Green: `DATABASE_URL=... bun test` on the three files: 17 pass 0 fail (118 expects); `check`, `build`, `bunx biome check` on the seven files, `git diff --check` all clean. `pendia-test-pg-26` still running.
- TODO4 done: red run failed with `Export named 'createShowsMedium' not found`. `shows.ts` now exports `showsScan` (pure ScanRules), `createShowsMedium(db)` (same metadata plus a `next-up` shelf whose `items({ userId })` calls `nextUp(db, userId)`, screens registered) and `nextUp` verbatim: `distinct on` highest completed `(season_number, episode_number)` per show, lateral first later non-completed candidate (partial Progress still eligible, Specials = season 0), ordered by last completed `played_at desc nulls last` then ids; unstarted and finished shows drop out. `scan.ts`/`jobs.ts` use `showsScan` for show walks. Green: focused `bun test` on shows/scan/jobs 32 pass 0 fail (194 expects), `check`, `build`, `bunx biome check` on the five files, `git diff --check` all clean. `pendia-test-pg-26` still running.
- TODO5 done: acceptance mapping is criterion 1 to `scan.test.ts` `writes a Show tree with split and ranged Episode Versions` (exact closure rows), criterion 2 to that test plus `shows.test.ts` grouping cases, criterion 3 to the two `next up` database tests in `shows.test.ts` (finished and unstarted shows omitted by fixture), criterion 4 to the 19 pure scan tests (Season N, Specials, split parts, ranges). README gained the `"movies" | "shows"` input, movie-or-show root fan-out, and the Sonarr layout paragraph. Gate: `bun install --frozen-lockfile` 0 (116 installs, no changes); `bun run lint` 0 (114 files); `bun run check` 0 (6 tasks); `bun run build` 0 (4 tasks); `DATABASE_URL=... bun test` 0 (510 pass, 0 fail, 1702 expects); `env -u DATABASE_URL bun test` 0 (363 pass, 147 skip, one skip banner); `env -u DATABASE_URL CI=true bun test apps/server/src/mediums/shows.test.ts` exit 1 with `DATABASE_URL is required for database tests in CI.` as designed. `git diff --check` clean; only README and plan modified. `bunx biome check` on the two Markdown files exits 1 only because Biome has no Markdown parser and no files were processed; `--no-errors-on-unmatched` exits 0 with no diagnostics and `bun run lint` covers the tree at 0. `pendia-test-pg-26` left running for review. Residual risk: none beyond the ignored-file exit quirk.
- PR58 batch: two real Pullfrog findings fixed. `PRRT_kwDOUQpMs86h5QfG`: retained Progress reached `nextUp` after an admin revoked library view through `libraryAccess`, and no caller re-filtered. Red: `excludes a library after view access is revoked` still returned the denied Episode after the deny row. Fix: `nextUp` computes `viewableLibraryIds` once, returns early when empty, selects `candidate.library_id as "libraryId"` (via `episode_item.library_id` in the lateral subquery) and filters rows in JS, keeping permission precedence single-sourced in `checkPermission`. `PRRT_kwDOUQpMs86h5QfI`: a later scan discovering an earlier or middle split part wrote final orders onto occupied ones under non-deferrable `files_version_order_unique`. Red: `reorders existing split Files when earlier and middle parts arrive` failed with Postgres `23505` on `files_version_order_unique`. Fix: after the Version is chosen, every existing File on it is shifted to `order + (maxOrder + members.length + 1)` before the final-order write, so the constraint cannot collide; stale Files stay high because reconciliation is deferred and File ids stay stable. Green: focused `bun test` on shows+scan 29 pass 0 fail; `bun run lint`/`check`/`build` 0; full `DATABASE_URL=... bun test` 512 pass 0 fail (1720 expects); `env -u DATABASE_URL bun test` 0 (363 pass, 149 skip, one banner); `git diff --check` clean. `pendia-test-pg-26` left running.
- PR58 follow-up `PRRT_kwDOUQpMs86h5VII`: the prior collision shift left each stale File above current members, so its order grew roughly by `maxOrder + members.length + 1` every rescan until int4 overflow. Red: `keeps stale File orders bounded across repeated scans` (part1 renamed off-disk, 32 rescans) failed with Postgres `22003 integer out of range` on the shift update. Fix: `versionFiles` now selects `id`, `path`, `order`; after the member loop writes orders `0..members.length - 1`, stale rows (paths absent from `versionGroup.paths`) are sorted by order then path then id and rewritten to the adjacent suffix `members.length + index`. Orders stay within `0..versionFiles.length + newFiles.length - 1`, rescans do not accumulate, File ids are stable, missing-path reconciliation stays deferred. Green: `DATABASE_URL=... bun test apps/server/src/libraries/scan.test.ts` 8 pass 0 fail (125 expects); `bun run lint`/`check`/`build` 0; full `DATABASE_URL=... bun test` 513 pass 0 fail (1728 expects); `env -u DATABASE_URL bun test` 0 (363 pass, 150 skip, one banner); `git diff --check` clean. `pendia-test-pg-26` left running.
- PR58 Codex batch (review 5190995830): `PRRT_kwDOUQpMs86h5Wmu` equivalent season spellings fixed. `Season 1` and `Season 01` passed the grammar but grouped by raw folder, so one show could carry two number-1 Seasons into `(showId, seasonNumber)` reuse. Red: `merges equivalent season folder spellings by number` saw 2 Seasons. Fix: `groupShowPaths` keys each Show's seasons map by `seasonNumber`, keeps the lexicographically first `seasonFolder` for `canonicalFolder`, and emits `seasons.values()`; Episode merge and Version grouping unchanged. `PRRT_kwDOUQpMs86h5Wms` season int4 bounds rejected as unreachable: `episodeTokenPattern` accepts `s(\d{1,2})` so filename seasons cap at 99 and every larger folder fails the filename/folder season match; verified `identify` returns null for `Season 2147483648` and `Season 999999999999999999999` and `groupShowPaths` skips them, while `Season 99`/`S99` still parses. No production or test change for it. Green: focused `bun test` 20 pass 3 skip without DATABASE_URL and 23 pass 0 fail with it; `bun run lint`/`check`/`build` 0; full `DATABASE_URL=... bun test` 514 pass 0 fail (1736 expects); `env -u DATABASE_URL bun test` 0 (364 pass, 150 skip, one banner); `git diff --check` clean. `pendia-test-pg-26` left running.
- PR58 follow-up `PRRT_kwDOUQpMs86h5bA8`: numeric Season grouping shares one Episode Version map, but split keys `stem:<stem>` omitted the raw folder, so parts in `Season 1` and `Season 01` merged into one Version. Red: pure test `keeps split Versions separate across equivalent season folders` saw 1 Version instead of 2; DB test `keeps equivalent season folder splits as distinct Versions` saw the second scan reuse 1 Version. Fix: `analyze` namespaces split identity as `stem:<seasonFolder>:<versionStem>`; `file:` keys, numeric Season merge, widest-range merging and persistence unchanged. Green: `bun test shows.test.ts` 21 pass 3 skip; `DATABASE_URL=... bun test` on shows+scan 33 pass 0 fail (235 expects); `bun run lint`/`check`/`build` 0; full suite 516 pass 0 fail (1765 expects); `env -u DATABASE_URL bun test` 0 (365 pass, 151 skip); `git diff --check` clean. `pendia-test-pg-26` left running.
- PR58 follow-up `PRRT_kwDOUQpMs86h5bPZ`: when `S01E01-E02` and `S01E01` formed one Episode 1-2 with two Versions and the ranged path later vanished, reconciliation being deferred kept its Version but the existing-Episode update narrowed `episodeEndNumber` to null, letting a stale ranged Version disagree with its Episode. Red: `preserves a retained range when its ranged path disappears` showed `episodeEndNumber` null after the ranged path renamed to `.removed`. Fix: the existing-Episode update now compares effective ends (`episodeEndNumber ?? episodeNumber` on both sides) and only updates when the discovered end is wider; equal or narrower discoveries leave the stored range. Green: `DATABASE_URL=... bun test apps/server/src/libraries/scan.test.ts` 10 pass 0 fail (157 expects); `bun run lint`/`check`/`build` 0; full suite 517 pass 0 fail (1778 expects); `env -u DATABASE_URL bun test` 0 (365 pass, 152 skip); `git diff --check` clean. `pendia-test-pg-26` left running.
- PR58 follow-up `PRRT_kwDOUQpMs86h5hjS`: after the widen-only fix, a retained Episode 1-2 whose ranged path vanished still blocked a later standalone `S01E02` insert with `23P01` on `episodes_season_range_exclude`, rolling back the whole scan. Red: extending `preserves a retained range when its ranged path disappears` with a third scan after adding `Show S01E02.mkv` failed with that exclusion error on the episode insert. Fix: in the existing-Episode branch, compute `overlapsDiscoveredEpisode` (a current grouped Episode whose start sits in the retained-only tail `discoveredEnd < n <= existingEnd`) and narrow `episodeEndNumber` to the discovered value when it is present, in addition to widening; ascending Episode order commits the narrow before the overlapping insert. Existing 1-2 with only current E1 still preserves the range. Green: focused scan tests 10 pass 0 fail (165 expects); `bun run lint`/`check`/`build` 0; full suite 517 pass 0 fail (1786 expects); `env -u DATABASE_URL bun test` 0 (365 pass, 152 skip); `git diff --check` clean. `pendia-test-pg-26` left running.
- PR58 Codex batch (review 5191110278): three of four findings fixed, one rejected. `PRRT_kwDOUQpMs86h5p39` widening into a retained standalone Episode: after `S01E01` and `S01E02` were persisted separately, a current `S01E01-E02` tried to widen Episode 1 to 1-2 and hit `23P01` on `episodes_season_range_exclude` against retained Episode 2. Red: `does not widen into a retained standalone Episode` failed with `23P01` on the `episodes` update. Fix: `scan.ts` loads `persistedEpisodes` (itemId + episodeNumber) once per Season after `seasonId` resolves, and `blocksWidening` rejects widening when another retained Episode starts in `existingEnd < n <= discoveredEnd`; narrowing stays as before and no retained rows are deleted. `PRRT_kwDOUQpMs86h5p4E` split extras: stripping `part1`/`pt2`/`cd3` markers hid `trailer`, `sample` and `featurette` suffixes from `isExtra`. Red: `rejects split extras after removing the part marker` saw `identify` accept all three. Fix: `analyze` reruns `isExtra` on the marker-stripped path before `episodeNumbers`; plain `Show S01E01 - part1.mkv` still parses. `PRRT_kwDOUQpMs86h5p4B` next-up not exposed: `createShowsMedium` registered the shelf but no procedure called it. Red: `serves the shows next up contribution over RPC and REST` failed with ORPC `NOT_FOUND`, and `openapi.test.ts` lacked `/shelves/next-up`. Fix: `marks.ts` adds an authenticated GET `shelves.nextUp` (output `Schema.Array(Schema.UUID)`) that finds the `next-up` shelf on `createShowsMedium(context.db)` and runs `shelf.items({ userId })` through `fromHost`/`runApi`; `.input` is omitted like `/me` and `/libraries` because an empty `Schema.Struct({})` fails the OpenAPI generator's GET input check. `openapi.test.ts` asserts the path, operation, 200 response and array-of-UUID schema. `PRRT_kwDOUQpMs86h5p3-` multi-file direct playback rejected as out of scope: #26 requires one Version with ordered Files; the direct-play planner belongs to #32, so playback code is unchanged. Green: `bun test shows.test.ts` 22 pass 3 skip 0 fail; `DATABASE_URL=... bun test scan.test.ts marks.test.ts` 17 pass 0 fail (230 expects); `bun test openapi.test.ts` 7 pass 0 fail; `bun run lint`/`check`/`build` 0; full `DATABASE_URL=... bun test` 565 pass 0 fail (2157 expects); `env -u DATABASE_URL bun test` 0 (367 pass, 198 skip, one banner); `git diff --check` clean. `pendia-test-pg-26` left running.
- This run is unattended. The lead owns design, review, commits, pushes, comments and the pull request. The built-in SWE-2 sidekick implements and validates each TODO from an exact brief.
- Work stays in `/home/mia/mia-cx/pendia/.worktrees/shows` on `feat/26-shows-medium`. The initial base is clean at `0e8ac31`, equal to `origin/main`.
- The repository does not define split-part spelling. This plan accepts the common terminal `partN`, `ptN` and `cdN` forms, separated by spaces, dots, underscores or hyphens. The part marker affects File order but not Version identity.
- A valid scanned episode sits under `<show>/Season N/` or `<show>/Specials/`. The filename season must match its folder. Files outside these folders are ignored. The first path segment is the canonical show folder, matching the requested Sonarr layout.
- Episode titles default to `Episode N` or `Episodes N-M`. Providers can replace them later. Filename quality words do not become metadata.
- Next up starts only after completed Progress exists for a show. A partially watched later Episode is still the candidate because `completed` defines watched. Specials participate as season zero.
- The database-bound medium factory closes over `Database` for next up. This keeps the published shelf callback shape `{ userId }` unchanged.
- Test seams are the Medium scan contract, show path grouping, `scanDirectory`, library service and jobs, and the next up shelf callback. These are the existing public module boundaries named by the issue.
- No schema migration is needed. The Show, Season, Episode, episode range, closure and Progress tables already exist.
- Before filing and before each review push, fetch and rebase on `origin/main`. Resolve conflicts in favor of merged code. Never force-push.
