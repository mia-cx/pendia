# #27 Keyframe index parsers and segment timeline at scan

## Summary

Read keyframes from Matroska Cues and MP4 sample tables without scanning packets. Cache the result with the probe. Persist each Version's index and lazy-index flag. Reuse playback's derivation and alignment checks when scans establish an Item's segment timeline.

## Acceptance criteria

- [x] Parsed keyframe times match ffprobe's full packet scan for start/end Cues and start/end moov fixtures.
- [x] Parser reads stay below 2 MB per fixture, including files larger than that budget.
- [x] Scans persist one timeline per Item cut, reuse it for later Versions, and record each Version's alignment.
- [x] Fragmented MP4 without a sync-sample table requests lazy indexing instead of failing the scan.

## TODOs

- [x] Add a bounded random-access reader and Matroska SeekHead/Cues parser. Validate generated start/end Cues fixtures against ffprobe, plus absent and malformed indexes.
- [x] Add MP4 sync-sample timing from stss, stts, ctts and supported edit lists. Validate start/end moov, B-frames, audio-first tracks and fragmented MP4 through the public parser.
- [x] Add Version keyframe storage and a lazy-index flag with a generated migration. Validate fresh and repeated migrations and index round trips against disposable Postgres.
- [x] Include the cheap index in probes and persistent probe-cache hits. Validate real probes, unchanged-file cache reuse, changed-file refresh and upgrading old cached results.
- [x] Persist scan timelines and Version alignment using playback helpers. Validate first-Version ownership, later aligned and unaligned Versions, independent cuts, rescans, concurrent scans and lazy fallback.
- [x] Complete large-payload fixture coverage and final validation. Assert fewer than 2,000,000 bytes read for every layout. Run all requested root checks and record actual results below.

## Notes

- PR59 review fixes (threads PRRT_kwDOUQpMs86h5aQq, PRRT_kwDOUQpMs86h5aQt): Matroska nonzero `CodecDelay` (0x56AA) now returns a lazy index instead of trusting unadjusted block timestamps, since codec-specific timestamp and ffprobe rounding make subtraction unsafe here. Cues are no longer trusted to reference random-access frames: each selected video cue requires `CueClusterPosition` and `CueRelativePosition` (no `CueBlockNumber` walks), rejects nonzero `CueCodecState` and any `CueReference`, and `isRandomAccessCue` verifies only cue-addressed container structure, meaning the Cluster element header, its `Timestamp` metadata scanned by declared element sizes until the first block, and the targeted SimpleBlock/BlockGroup prefix (track VINT, signed timecode, flags). Keyframe flag, no lacing, selected track, no ReferenceBlock/CodecState in groups, and `clusterTimestamp + blockTimecode === cueTime` must all hold or the index goes lazy. No media payloads or sequential packet scans are read; this is a bounded index-directed metadata check under the same 2,000,000-byte budget. Current measured reads: large mkv 752/753 B, large mp4 429/445 B, small mkv 732/733 B, small mp4 429/445 B, all far under budget. Validation: keyframes suites 31 pass, DB regression 38 pass, lint/check/build clean. Evidence: `node_modules/.cache/pendia-27/review-fix-validation.txt`. Observed oracle: the real non-RAP coverage retargets a generated cue at a genuine second-cluster interframe (cue time 0.04 s, outside every keyframe time) and the parser still refuses it, while ffprobe reports packet times from the H.264 stream rather than the container flag; a 1s CodecDelay remux returns the original packet times.
- Final root validation: `bun install --frozen-lockfile` passes without changes; `bun run lint` passes (123 files); `bun run check` passes; `bun run build` passes (4 tasks). `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55427/pendia bun test` passes (524 tests, zero failures). `env -u DATABASE_URL -u CI bun test` passes (372 pass, 152 skip, zero failures). `env -u DATABASE_URL CI=1 bun test apps/server/src/db/db.test.ts` fails as expected with the required DATABASE_URL message.

- TODO6 done: `large` fixture option (640x360, `-crf 0`) and `keyframes/fixtures.test.ts` four-case matrix asserting size over 2 MB, bytesRead under 2,000,000 and oracle equality per PTS. Observed: mkv end 5,859,042 B file / 753 B read, mkv start 5,867,111 B / 752 B, mp4 end 5,857,977 B / 445 B, mp4 start 5,857,977 B / 429 B. Generated MKV B-frame and audio-first cases match the packet oracle. The unreachable front-Cues case now includes valid Info and Tracks. README scan paragraph updated. Final gate results recorded in `node_modules/.cache/pendia-27/27-final-validation.txt`.
- TODO5 done: scan.ts persists `keyframesSeconds`/`lazyIndexPending` on Version insert and update, then `persistScanTimelines(tx, itemId)` inside the locked transaction derives each cut's timeline from its first imported Version only and stamps per-Version alignment. `persistScanTimelines` takes the tx type since tx lacks `$client`. `bun test` scan + timelines 11 pass, tsc and biome clean. Anomaly: createVideoFixture MKV indexes `[0]` not a nonzero first PTS, so scan.test.ts expects aligned timelines; the nonzero-first contract uses an `-output_ts_offset 5` MKV (`[5,7]`, index stored, no timeline).
- TODO4 done: `ProbeResult.keyframesSeconds`; `probeVideo` appends `readKeyframeIndex` after the unchanged header-only ffprobe decode; probe-cache re-probes once when the cached result lacks the property (null is a valid cached negative). `bun test` probe + probe-cache 27 pass, tsc and biome clean. Anomaly: `probe_cache.result` is stored as a jsonb string scalar, so the legacy strip uses `to_jsonb(((result #>> '{}')::jsonb - 'keyframesSeconds')::text)`; a plain `result - key` fails with "cannot delete from scalar".
- TODO3 done: additive migration `0003_nifty_sunspot.sql` adds `keyframes_seconds double precision[]` and `lazy_index_pending boolean not null default true`; db.test.ts journal counts 3 to 4 (34 tables) plus a defaults/persist/reset round trip. `bun test db.test.ts` 9 pass against `pendia-test-pg-27` on 127.0.0.1:55427, tsc and biome clean.
- TODO2 done: `readMp4Keyframes` over IndexReader (moov at either end, mvex lazy, first `vide` trak, stss plus stts required, ctts v0/v1 cursor, restricted elst, media-interval and negative-PTS guards) with ftyp dispatch; mp4 tests in `keyframes/mp4.test.ts`. `bun test` keyframes + mp4 20 pass, tsc and biome clean. Reads 156 to 2,109 bytes. Review fixes: nonzero stss/stts versions rejected, zero stts deltas rejected, negative or non-finite PTS goes lazy.
- TODO1 done: `readKeyframeIndex` dispatch, `FileIndexReader` (2,000,000 actual-byte budget checked before IO) and Matroska SeekHead/Cues parser. `bun test keyframes.test.ts` 9 pass, tsc and biome clean. Observed reads 530/529 bytes on ~440 KB fixtures. Review fixes: budget rejects before IO, integers cap at 8 bytes, Cues only via SeekHead, non-1 TrackTimestampScale goes lazy, exact VINT header reads.
- Work stays on `feat/27-keyframe-index` in the supplied worktree. Each TODO gets its own reviewed implementation commit with `Refs #27`.
- Owned code is the new keyframe module, video probe, probe cache, scan pipeline, Version schema and migration metadata. Existing playback derivation stays unchanged.
- Test seams are the public file index reader, `probeVideo`, `probeLibraryFile`, `scanDirectory`, and migrated database storage. Database assertions verify persistence required by this issue.
- Use the first timed video track. Return presentation timestamps in seconds, matching ffprobe packet PTS. MP4 composition offsets and ordinary rate-one edit lists must be applied.
- Index reads have a 2,000,000-byte budget. Missing, unsupported, malformed or oversized indexes return a lazy result. Filesystem errors still fail the probe. No production ffprobe packet scan is added.
- The first persisted imported Version owns derivation for its cut. Initial directory ordering is deterministic. A later Version must not replace an unavailable first Version as the timeline source.
- Reuse `editionTag` for cut identity, normalized to lowercase; untagged Versions use `original`. Existing timeline identities and boundaries remain immutable.
- Playback requires keyframes beginning at zero. Preserve raw container times. An index with a nonzero first keyframe remains stored but cannot establish a timeline with this playback contract. Do not fabricate a keyframe at zero.
- Lazy indexing execution on first play belongs to playback delivery. This slice persists the request flag only.
- Tests use the existing disposable-database helper. Local Postgres is `pendia-test-pg-27` on port 55427 and is removed at completion.
- Rebase on `origin/main` before filing and before each push. Never force-push. If a later rebase would require rewriting published history, report the constraint rather than override it.
- Source references read: `CONTEXT.md`, `docs/research/ffmpeg-hls-pipeline.md`, `docs/spec/playback.md`, `docs/spec/plugin-api.d.ts`, ADRs 0002, 0006, 0008 and 0009, server README, playback timeline/adaptive modules and tests, video probe and fixtures, and scan/cache/schema modules and tests.
