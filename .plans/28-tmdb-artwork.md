# #28 TMDB provider, artwork store and artwork serving

## Summary

Add the in-tree TMDB provider for movies and connect it to scans through the provider-fetch job. Match by a stored TMDB provider id or by title and year above a confidence threshold. Persist matched metadata, Contributors, Credits and one selected poster. Store the original poster in the Item's colocated `.pendia` folder. Serve resized artwork with Sharp, an ETag, a process-local cache and the auth setting from the artwork spec.

## Acceptance criteria

- [x] A scanned fixture movie receives TMDB metadata, credits and a poster; an ambiguous title lands in the unmatched state.
- [x] Artwork is served resized with an ETag, and a repeat request hits the local cache.
- [x] Anonymous artwork works by default and the toggle enforces auth.
- [x] Provider calls are mocked in tests; the TMDB key lives in settings.

## TODOs

- [x] 1. Add metadata and artwork settings plus persisted match state.
  - Add an Item metadata state with `pending`, `matched` and `unmatched` values. Generate and review the Drizzle migration.
  - Read the `metadata` settings row as `{ providerOrder, confidenceThreshold, libraries, tmdb }`. Missing library entries enable the ordered providers. An empty library list disables metadata for that library.
  - Add `artworkRequiresAuth`, defaulting to false, to the existing `auth` settings object.
  - Validation: disposable Postgres tests cover defaults, configured provider order, per-library enablement, the TMDB key, the confidence threshold and artwork auth validation. Run server typecheck and build.
- [x] 2. Implement the TMDB movie provider on the plugin metadata contract.
  - Implement `MetadataProvider.search` and `MetadataProvider.fetch` for movies. Decode TMDB responses at the HTTP boundary without `any`.
  - Search by title and optional year. Derive confidence from normalized title and year agreement. Fetch details, credits, content rating, provider ids and original artwork URLs.
  - Validation: mocked HTTP tests cover request parameters, decoded matches, metadata, Contributors and Credits input, provider ids, artwork URLs and malformed or failed responses. Run server typecheck and build.
- [x] 3. Match and persist movie metadata in provider order.
  - Add a metadata service that tries enabled providers in configured order. Use an existing provider id before search. Accept one unique best result only when it meets the configured threshold. Persist `unmatched` otherwise.
  - On a match, replace provider-owned Item metadata, provider ids and Credits transactionally. Reuse Contributors by exact name when possible.
  - Validation: disposable Postgres tests cover provider-id matching, title-year matching, provider order, disabled libraries, threshold rejection, ambiguous best results, idempotent Credits and unmatched state. Run server typecheck and build.
- [x] 4. Connect scans to provider jobs and store poster originals colocated.
  - Parse Radarr provider suffixes into provider-id rows during a movie scan. Enqueue one provider-fetch job after each successful directory scan and register its built-in worker handler without overriding supplied handlers.
  - Download the selected poster through the injected provider HTTP client. Store its original bytes at `<canonical-folder>/.pendia/artwork/<artwork-id>` and persist the selected colocated artwork row.
  - Validation: a real scan fixture plus mocked provider HTTP calls receives metadata, Credits, provider ids and a poster in its Item folder. Job tests cover registration, retries through thrown failures and no provider call when the library disables providers. Run server typecheck and build.
- [x] 5. Serve resized artwork with cache, ETag and optional auth.
  - Add Sharp at a pinned release older than seven days. Serve `GET /api/artwork/{id}?width=<pixels>` from the API role for selected colocated artwork.
  - Validate width and storage paths. Resize without enlargement. Return an ETag and honor `If-None-Match`. Cache resized bytes per process and invalidate them when the original changes.
  - Allow anonymous requests by default. When `artworkRequiresAuth` is true, require the existing bearer token or session cookie.
  - Validation: real HTTP and Sharp tests cover dimensions, ETag and 304 behavior, one resize across repeat requests, cache invalidation, invalid input, anonymous default and enforced auth. Run server typecheck and build.
- [x] 6. Complete acceptance coverage and run final repository checks.
  - Review each acceptance criterion against public-boundary tests. Document settings, matching, the provider-fetch job, the colocated artwork layout and the artwork route in `apps/server/README.md`.
  - Run from the repository root: `bun install --frozen-lockfile`, `bun run lint`, `bun run check`, `bun run build`, `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test`, and `env -u DATABASE_URL bun test`.
  - Confirm database tests fail in CI without `DATABASE_URL`. Remove only the task's disposable Postgres container after the review loop ends.
  - Validation: all commands pass, local database omission prints the existing single skip message, and Notes record real results and blockers.

## Notes

- This run is unattended. The lead owns design, review, commits, pushes, comments and the pull request. The authorized SWE-2 sidekick implements each TODO and runs its focused checks. No other agent or CLI worker runs.
- Work stays in `/home/mia/mia-cx/pendia/.worktrees/tmdb` on `feat/28-tmdb-artwork`. Each TODO gets one buildable commit with `Refs #28`. Never commit `.devin`.
- Test seams are metadata settings, the plugin `MetadataProvider` contract, metadata application, queue dispatch and the artwork HTTP route. These are the repository's public module boundaries. This unattended plan fixes those seams before tests are written.
- The repository does not name a settings JSON shape. Use the `metadata` row with `providerOrder: string[]`, `confidenceThreshold: number`, `libraries: Record<libraryId, string[]>`, and `tmdb: { apiKey: string } | null`. Defaults are `['tmdb']`, `0.9`, inherited ordered providers per library and no key. An explicit empty library list disables providers.
- The repository does not name the auth field. Use `artworkRequiresAuth: boolean`, default false, because it states the enforced behavior directly.
- A unique highest-confidence result at or above the threshold matches. Tied best results are ambiguous and become `unmatched`, even above the threshold.
- TMDB folder ids take the exact-match path. Other parsed provider ids remain stored, but this slice has no provider that can resolve them.
- The colocated storage key is library-relative and remains under `<canonical-folder>/.pendia/artwork/`. Configured-path and S3 backends remain future work, as issue #28 asks for the colocated backend.
- The artwork route uses the artwork row id. Width is a required positive integer capped at 4096. Sharp preserves the source format and never enlarges it.
- Local Postgres uses the requested `pendia-test-pg-28` container and port 55428. Tests create disposable databases and leave the named database untouched.
- Before filing and before every review push, rebase on `origin/main`. Resolve conflicts in favor of merged code. Never force-push.
- Initial repository state is clean at `0e8ac31`, equal to `origin/main`. Issue #25 is closed and its scan pipeline is present.
- TODO 1 done. Added `metadata_state` enum and `items.metadata_state` default `pending` in `src/db/schema/core.ts` with migration `0004_pale_carmella_unuscione` (creates the enum, adds the column with `DEFAULT 'pending' NOT NULL`, backfilling existing rows). New `src/metadata/settings.ts` reads the `metadata` row with the documented defaults and validates every malformed value as `Invalid metadata settings.`; `providersForLibrary` returns fresh arrays in configured order. `readAuthSettings` in `src/auth/settings.ts` now returns `artworkRequiresAuth`, default false, non-booleans rejected. Tests in `src/metadata/settings.test.ts`, `src/auth/settings.test.ts`, `src/auth/rate-limit.test.ts` and `src/db/db.test.ts` (journal count 5, `metadata_state` column default proven). Red observed first: missing module, missing `artworkRequiresAuth`, missing default. `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/settings.test.ts apps/server/src/auth/settings.test.ts apps/server/src/auth/rate-limit.test.ts apps/server/src/db/db.test.ts` passed 26 of 26; `bun run --cwd apps/server check` and `bun run --cwd apps/server build` clean; `bunx biome check` clean after formatting two files. Review fixes: `libraries: null` now rejects instead of defaulting, rate-limit assertions gained `artworkRequiresAuth`, malformed-case array typed `JsonValue[]`, preserved unknown fields asserted with `toMatchObject`.
- TODO 2 done. New `src/metadata/tmdb.ts` exports `createTmdbMetadataProvider(apiKey, request?)` returning a `MetadataProvider` for movies: search hits `/search/movie` with settled confidence rules (0.8 exact normalized title else 0.5, plus 0.1 without query year or 0.2 on year match, capped at 1), fetch hits `/movie/<id>` with `append_to_response=credits,release_dates,external_ids,images` and decodes details, cast/crew credits with role-local order, US certification, deduped genres and artwork, and tmdb/imdb provider ids. Errors never carry bodies, URLs or the key: `TMDB request failed with status <n>.` on non-2xx and `Invalid TMDB response.` on bad JSON or shape. Added `@pendia/plugin-api` `workspace:*` to `apps/server/package.json` (bun.lock updated) so the type import resolves. Tests in `src/metadata/tmdb.test.ts` use an injected `typeof fetch` mock only. Red observed first: `Cannot find module './tmdb.ts'`. `bun test apps/server/src/metadata/tmdb.test.ts` passed 10 of 10; `bun run --cwd apps/server check` and `bun run --cwd apps/server build` clean; `bunx biome check` clean after formatting both new files. No behavior fixes needed after green.
- TODO 3 done. New `src/metadata/service.ts` exports `applyMetadata(db, itemId, providers)` and the `MetadataApplication` union. It reads metadata settings, walks enabled provider ids in configured order, skips unregistered providers and wrong kinds, uses a stored provider id for direct fetch at confidence 1, otherwise searches and accepts only a unique best match at or above the threshold. Matched results persist in one transaction under a `FOR UPDATE` item lock: item fields updated, item provider ids replaced with trimmed entries (blank names or values reject with `Invalid provider metadata.` before the transaction), credits replaced with contributors reused by exact name in stable id order or created once per name, and the fetched artwork returned as a copy. No match leaves the item `unmatched` through a private `persistUnmatched` that re-reads the item under the same `FOR UPDATE` lock, so both final states serialize identically and a deleted item rejects `AuthError("NOT_FOUND")` instead of storing unmatched. `items.kind` narrowed to the plugin `ItemKind` union directly, so no cast was needed. Tests in `src/metadata/service.test.ts` run against disposable Postgres with mocked providers only. `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/service.test.ts` passed 11 of 11; `bun run --cwd apps/server check` and `bun run --cwd apps/server build` clean; `bunx biome check` clean after import sorting and formatting. Red was not run separately; the first green run failed once on a stale test call count, fixed by asserting the recorded count. Other review fixes were test-side typing of the malformed provider-id array. Lead review fix: the unmatched path originally wrote without the item lock; it now shares the serialized re-read, proven by a regression test that deletes the item mid-search using explicit promises.
- TODO 4 done. `src/mediums/movies.ts` gained a private `folderProviderIds` that shares one suffix source with `parse` for `{tmdb-348}`/`{imdb-tt0078748}`/`{tvdb=123}` Radarr suffixes (case-insensitive, trimmed values, first non-empty per provider, empties ignored), and `MoviePathGroup` gained `providerIds` while `parse` keeps returning only `{ title, year }`. `src/libraries/scan.ts` upserts the folder's parsed ids inside the existing library-locked transaction (read-then-update/insert, never deletes absent providers). `src/db/schema/operations.ts` dropped `provider` from the `provider-fetch` payload (no migration; JSON shape only). `src/libraries/jobs.ts` enqueues one `{ type: "provider-fetch", itemId }` after a successful non-empty directory scan; root fan-out is unchanged. New `src/metadata/artwork-store.ts` exports `storeArtworkOriginal(db, itemId, candidate, request?)`: it fetches the URL once with `accept: image/*`, rejects non-2xx as `Artwork request failed with status <n>.`, writes through a same-directory temp file plus rename to `<canonicalFolder>/.pendia/artwork/<artwork-id>`, validates storage keys against absolute paths, NUL, dot segments and escapes of the library root (`Invalid artwork storage path.`, reused keys too), then in one transaction unselects same-type Item artwork and updates or inserts the selected colocated row. New `src/metadata/jobs.ts` exports `registerMetadataJobs(db, registry, request?)`, which builds the TMDB provider only when `config.tmdb` is set, applies metadata, and stores the first poster candidate. `src/index.ts` now registers the scan and provider-fetch built-ins independently so each supplied handler wins on its own. Tests: `movies.test.ts` gained provider-id extraction cases and existing group expectations gained `providerIds`; `walker.test.ts` group expectation gained `providerIds`; `scan.test.ts` asserts the `{tmdb-348}` row and that repeat scans reuse it; `libraries/jobs.test.ts` proves one provider-fetch enqueue per directory scan and none for an empty directory; new `artwork-store.test.ts` and `metadata/jobs.test.ts` use disposable Postgres, a temp library root and injected fetch mocks only, covering exact bytes, headers, reuse, non-2xx, invalid reused keys, the stored-id TMDB flow with the key confined to the TMDB query, disabled libraries making no calls, thrown-request retry leaving one selected row, and single-type registration; `index.test.ts` proves supplied scan and provider-fetch handlers each win while the other built-in still registers. `bun test apps/server/src/mediums/movies.test.ts` passed 39 of 39. `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/libraries/scan.test.ts apps/server/src/libraries/jobs.test.ts apps/server/src/metadata/artwork-store.test.ts apps/server/src/metadata/jobs.test.ts` passed 21 of 21; with `walker.test.ts` and `index.test.ts` added the run passed 41 of 41. `bun run --cwd apps/server check` and `bun run --cwd apps/server build` clean; `bunx biome check` clean after formatting. Review fixes: `walker.test.ts` needed `providerIds: {}` in a full-object expectation, the `poster` fixture needed `as const` for the artwork type union, byte assertions use `Buffer`, and item lookups gained non-null throws for strict typing. Lead review fixes: `metadata/jobs.test.ts` now runs the full scan-to-poster chain (a real `Alien (1979) {tmdb-550}` fixture video is scanned, the parsed TMDB id drives direct fetch, and the enqueued provider-fetch stores the colocated poster) plus a `King Kong (1933)` ambiguity regression that ties two best search results and asserts unmatched with exactly one search call; and `artwork-store.ts` replaced the string-prefix containment check with `relative`/`isAbsolute` and gained `ensureRealDirectory`, which lstat-validates the root and every parent component as a real directory (rejecting symlinks, creating missing components one at a time, and re-validating on EEXIST races) before the temp write, so a `.pendia` symlink can no longer redirect artwork outside the library. The symlink regression proves rejection with no artwork row and no write into the link target. After the rework the focused runs pass: `bun test apps/server/src/mediums/movies.test.ts` 39 of 39; the six-file database run (scan, library jobs, artwork-store, metadata jobs, walker, index) 43 of 43; check, build and biome clean.
- TODO 5 done. Added `sharp@0.35.4` via `bun add` (published 2026-08-26, more than seven days before this run); `apps/server/package.json` and `bun.lock` updated. `src/metadata/artwork-store.ts` now exports `ArtworkOriginal` and `readArtworkOriginal(db, artworkId)`, which returns null for missing, non-item-owned or non-colocated rows and reuses `resolveStoragePath`; the private parent walker became `walkStorageDirectory(root, dir, create)` so store mode still creates missing components one at a time while read mode only lstat-validates real non-symlink directories, then lstat-checks the leaf (ENOENT returns null, symlink or non-regular file throws `Invalid artwork storage path.`). New `src/metadata/artwork-http.ts` exports `ArtworkResize` and `createArtworkHandler(db, {resize?, maxCacheEntries? = 128})`: paths under `/api/artwork/` need exactly one UUID segment (400 otherwise), only GET (405 + `Allow: GET`), `readAuthSettings` on every matching request with `authenticate(db, readSessionToken(request))` when `artworkRequiresAuth`, width validated as one ASCII-digit safe integer 1..4096, then `readArtworkOriginal` (404 on null). The ETag is a quoted SHA-256 over the original bytes plus `;w=<width>`; `If-None-Match` honors `*` and exact tags but not weak tags, returning 304 with ETag and cache headers. Resized results sit in an insertion-ordered Map keyed by that ETag, refreshed on hit and evicting the oldest key past the bound; default resize keeps the source format via sharp `{ width, withoutEnlargement: true }` with a MIME table (`application/octet-stream` fallback). 200s carry Content-Type, Content-Length, ETag, `Cache-Control: public, max-age=86400` and nosniff; JSON errors use `{ error: { code, message } }` with `no-store`, and unexpected failures log only `{ level: "error", message: "artwork.request.failed" }` before a generic 500. `src/api.ts` gained the optional `artwork` handler, dispatched after auth and before the oRPC/API handler, returning only defined responses; `src/index.ts` constructs one handler per API process. Tests: `artwork-store.test.ts` gained five read-mode cases (round-trip, missing id/file, non-colocated row, leaf and parent symlink rejection); new `artwork-http.test.ts` proves a real Sharp 4x4 PNG resize and 8x8 no-enlargement, ETag/304/weak-tag/LRU/content-invalidation behavior through an injected resize, anonymous default plus `artworkRequiresAuth` enforcement with a real admin API key, path/width/method validation and generic 500s; `api.test.ts` and `index.test.ts` needed no changes and still pass. `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/artwork-store.test.ts apps/server/src/metadata/artwork-http.test.ts apps/server/src/api.test.ts apps/server/src/index.test.ts` passed 32 of 32 (113 expects); `bun run --cwd apps/server check` and `bun run --cwd apps/server build` clean; `bunx biome check` clean after `--write` formatting. Red was not run separately; first tsc pass flagged the injected fetch casts and a `Uint8Array` body needing `Buffer.from`, both fixed before green.
- TODO 6 done. `apps/server/README.md` gained `artworkRequiresAuth` in the auth defaults block plus its anonymous-or-credential prose, and a `## Metadata and artwork` section documenting the `metadata` settings shape, provider defaults and per-library disablement, folder-suffix provider ids, scan to provider-fetch flow, matching and persistence rules, the colocated `.pendia/artwork` layout, and `GET /api/artwork/{id}?width=1..4096` (Sharp no-enlargement, strong ETag, If-None-Match, 128-entry per-process cache, anonymous by default). All four acceptance criteria are checked against named public tests: `metadata/jobs.test.ts` (scanned Alien fixture receives metadata, Credits and poster; scanned King Kong ambiguity lands unmatched), `artwork-http.test.ts` (real Sharp resize, ETag, 304, cache counting, anonymous default and real API key enforcement), and `tmdb.test.ts`/`metadata/jobs.test.ts`/`metadata/settings.test.ts` (mocked provider calls, key read from settings). Final gate results: `bun install --frozen-lockfile` exit 0 (123 installs, no changes); `bun run lint` exit 0 after `bunx biome check --write` reformatted the branch's own unformatted `drizzle/meta/0003_snapshot.json` and `_journal.json` (fix for a this-branch defect); `bun run check` exit 0 (6 turbo tasks); `bun run build` exit 0 (4 turbo tasks); `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test` exit 0, 544 pass / 0 fail / 1955 expects across 40 files; `env -u DATABASE_URL bun test` exit 0, 357 pass / 187 skip / 0 fail with the single skip message `Skipping database tests: set DATABASE_URL to a test Postgres server.`; `env -u DATABASE_URL CI=true bun test apps/server/src/metadata/settings.test.ts` exit 1 with `DATABASE_URL is required for database tests in CI.` as designed. Bun 1.4.2, turbo 2.10.12. Lead review fix: the scan section's stale `providers ... belong to later slices` sentence was corrected to name only missing-file reconciliation, change signals and keyframe indexes; `bun run lint` and `git diff --check` re-verified clean. No other fixes needed.
- PR review fix batch on `224c32e`, uncommitted. Ten real findings fixed with tests. `artwork-http.ts` picks `private, max-age=0, must-revalidate` when `artworkRequiresAuth` is set and `public, max-age=0, must-revalidate` otherwise, applied to 200 and 304 so enabling the toggle reaches the origin; the process LRU is unchanged. `libraries/jobs.ts` enqueues provider-fetch with `concurrencyKey: provider:<itemId>`, and `storeArtworkOriginal` locks the Item `FOR UPDATE` inside one transaction covering selected-row lookup, filesystem write and row update, after download and Sharp validation. `metadata/jobs.ts` publishes `library.changed` after matched and unmatched writes, throws `AuthError("NOT_FOUND")` when the Item vanished, and emits nothing before a retried failure. `ItemCard` gained `posterArtworkId` through a correlated subquery and detail inherits it. `service.ts` upserts only fetched provider ids and preserves other scanned ids; Contributor creation takes sorted `pg_advisory_xact_lock(hashtextextended(name, 0))` locks before select/create. `movies.ts` validates suffix values (`tmdb`/`tvdb` positive decimal, `imdb` `tt[1-9][0-9]*` normalized lowercase); malformed values still strip from the title but store nothing, and a later valid duplicate wins. `readArtworkOriginal` returns null for deselected rows. Downloads are Sharp-validated before storage with `Invalid artwork response.` on malformed content, and decoded width/height persist on the row. The Pullfrog symlink-swap claim was rejected: no repository actor replaces a validated `.pendia` parent with a symlink between adjacent operations, and static parent and leaf symlinks already reject. Two duplicate public-caching reports and two duplicate missing-event reports folded into the same fixes. Implementation corrections during verification: drizzle renders interpolated columns unqualified inside the `sql` subquery, so the correlated clause compared `artwork.item_id` to `artwork.id`; the outer reference is now the literal `"items"."id"`. Two test-only typecheck fixes: the concurrent fetch mock needed the `(input, init)` signature to overlap `typeof fetch`, and a `string | undefined` expectation was narrowed to a thrown non-null. `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/artwork-http.test.ts apps/server/src/metadata/artwork-store.test.ts apps/server/src/metadata/jobs.test.ts apps/server/src/metadata/service.test.ts apps/server/src/libraries/jobs.test.ts apps/server/src/libraries/scan.test.ts apps/server/src/mediums/movies.test.ts apps/server/src/api/router.test.ts apps/server/src/api/client.test.ts` exit 0, 109 pass / 0 fail / 466 expects; `bun run lint`, `bun run check` and `bun run build` each exit 0. README updated for cache revalidation, provider-id upserts, `posterArtworkId` and the completion event.
- Pullfrog follow-up on `2813365`, uncommitted. The IMDb folder-suffix pattern `/^tt[1-9][0-9]*$/i` rejected the repository's standard zero-padded id `tt0078748`. `movies.ts` now uses `/^tt0*[1-9][0-9]*$/i`, which accepts leading zeros but still rejects all-zero ids. `movies.test.ts` restored the canonical `tt0078748` extraction fixture, uses uppercase `TT0078748` for the normalization case, and the malformed test rejects `tt0` and `tt0000000` before accepting a later `tt0078748`. `bun test apps/server/src/mediums/movies.test.ts` exit 0, 40 pass / 0 fail / 103 expects; `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/libraries/scan.test.ts` exit 0, 6 pass / 0 fail / 57 expects; `bun run lint`, `bun run check` and `bun run build` each exit 0.
- Rebase onto new `origin/main` `cf3a7c0`: main claimed migration index 0003 (`0003_nifty_sunspot`, keyframes/timelines). Main's 0003 SQL, snapshot and journal entry were kept; the superseded `0003_nifty_mad_thinker` was removed and `bun run --cwd apps/server db:generate` regenerated the metadata change as `0004_pale_carmella_unuscione` (only the `metadata_state` enum and `items.metadata_state` default `pending`). `db.test.ts` journal assertions moved to 5. Post-rebase validation: `bun run lint`, `bun run check` and `bun run build` each exit 0; `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/db/db.test.ts apps/server/src/metadata/settings.test.ts apps/server/src/mediums/movies.test.ts apps/server/src/libraries/scan.test.ts apps/server/src/api/marks.test.ts` exits 0 with 66 pass, 0 fail and 345 expects.
- Rebase onto new `origin/main` `692f098` (shows medium): movie directory scans enqueue `provider-fetch` while show directory scans use `scanShowDirectory` without provider work; the `provider:<itemId>` concurrency key applies to the movie enqueue only. Main's `shelves/next-up` returns episode id strings, so `posterArtworkId` needed no next-up integration and stays on ItemCard, item list/detail and continue watching. Migration chain kept as 0003 keyframes then 0004 metadata. Validation: `bun run lint`, `bun run check` and `bun run build` each exit 0; `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test` on scan, library jobs, metadata jobs, router and marks tests exits 0 with 47 pass / 0 fail / 373 expects, plus `mediums/shows.test.ts` 25 pass / 0 fail / 98 expects.
- Codex follow-up on `d4e6c1f`, uncommitted. Two real findings fixed with tests; the symlink TOCTOU thread was rejected as a duplicate of the already rejected speculative finding (no repository actor replaces a validated `.pendia` path with a symlink between adjacent calls; static parent and leaf symlinks already reject). `tmdb.ts` and `artwork-store.ts` gained a `timeoutMs = 30_000` parameter validated as a positive safe integer (`Invalid TMDB request timeout.` / `Invalid artwork request timeout.`); each request creates one `AbortSignal.timeout(timeoutMs)` passed with the accept header, and the same signal covers `response.json()`/`response.arrayBuffer()` body reads. Timeout errors propagate unchanged to the queue retry path and leave no row or file. `artwork-http.ts` gained `maxCacheBytes` (default `64 * 1024 * 1024`, same `Invalid artwork cache size.` on invalid bounds) with retained-byte tracking, oldest-first eviction until both entry and byte bounds hold, no retention of oversized single entries, and LRU hits leaving byte totals unchanged. Requested widths now canonicalize to `min(width, original width)` before ETag, cache key and resize, so requests at or above the original share one representation and ETag; stored width `null` falls back to the requested width. README states the 128-representation and 64 MiB defaults with per-request revalidation. Tests: injected `init.signal` asserts `AbortSignal` on search, fetch and artwork requests; deterministic abort-driven timeouts with `timeoutMs = 1` prove rejection and no DB/file changes; invalid-timeout cases cover both modules; widths 20 and 30 on an 8px original share one ETag and one resize call at effective width 8; a 10-byte budget evicts a 6-byte entry before the count bound; an oversized entry is never cached. `bun test apps/server/src/metadata/tmdb.test.ts` exit 0, 12 pass / 0 fail / 139 expects; `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/artwork-store.test.ts apps/server/src/metadata/artwork-http.test.ts apps/server/src/metadata/jobs.test.ts` exit 0, 33 pass / 0 fail / 175 expects; `bun run lint`, `bun run check` and `bun run build` each exit 0; `git diff --check` clean.
- Pullfrog follow-up on `5d0265f`, uncommitted. Two real findings fixed with tests. `artwork-http.ts` adds an ETag-keyed in-flight map so concurrent misses share one resize Promise; the entry is removed on settle only while the map still points at that Promise, each waiter re-checks the cache after awaiting (a shared hit refreshes position without touching `cacheBytes`), only the first waiter still seeing a miss inserts and runs the count/byte eviction loop, and a rejection clears the entry so the next request retries. Oversized shared results are still never retained. `tmdb.ts` `requestJson` now binds the `AbortSignal.timeout(timeoutMs)` to a local passed in init, and the `response.json()` catch rethrows `signal.reason` when aborted, so a stalled body surfaces the timeout reason instead of `Invalid TMDB response.`; only real parse failures stay `Invalid TMDB response.`. Tests: a gated injected resize proves two concurrent same-ETag requests make one resize call with identical bodies and ETags and a third request stays cached; a first-attempt rejection returns 500 then retries to success (`[4, 4]`); a Response-shaped stub with an abort-observing `json()` proves the `TimeoutError` reason propagates at `timeoutMs = 1`. `bun test apps/server/src/metadata/tmdb.test.ts` exit 0, 13 pass / 0 fail / 141 expects; `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/artwork-http.test.ts` exit 0, 13 pass / 0 fail / 91 expects; `bun run lint`, `bun run check` and `bun run build` each exit 0 after one Biome reflow of the eviction loop; `git diff --check` clean.
- Final Codex batch on `4819166`, uncommitted. Five real findings fixed with tests. `artwork-store.ts` gained `maxDownloadBytes = 32 * 1024 * 1024` (positive safe integer, `Invalid artwork download limit.` before HTTP) and a bounded stream reader: an oversized decimal Content-Length cancels the body and throws `Artwork response too large.` before retaining bytes, malformed lengths fall back to streaming enforcement with `getReader()` accounting, reader lock always released, combined buffer allocated only after EOF, and the request AbortSignal still governs reads. Downloads are now fully decoded: one Sharp input reads metadata then `stats()` proves pixel decode, so a truncated PNG that yields positive dimensions still fails `Invalid artwork response.` before any row or file change. Replacements keep the artwork row id but write fresh generations `<artwork-id>.<generation-uuid>` under the same temp+rename path, switch the row inside the Item-locked transaction, and remove the old target best-effort only after commit, so a rollback leaves the old row pointing at untouched bytes with at most an unreferenced orphan. `jobs.ts` publishes `library.changed` once after `applyMetadata` commits (matched or unmatched) and again after poster storage, so a poster failure preserves the metadata event while the error still triggers queue retry. `artwork-http.ts` keys cache and in-flight maps by a private source hash and derives the response ETag from SHA-256 over the served content type and resized bytes; `If-None-Match` now evaluates against the output-derived tag after the result exists, so cold conditional requests may resize once and encoder changes invalidate correctly. Tests cover both download-limit paths with cancel assertions, invalid limits, the metadata-only-but-undecodable regression, generation-keyed sequential and concurrent replacement, a check-constraint rollback keeping the old row and bytes, three events for the matched scan flow, two for a provider failure retried to success, one metadata event plus zero artwork on a 503 poster with retry recovery, different output bytes producing different ETags across handlers, and a cold `If-None-Match` resizing exactly once before 304. The rejected symlink race remains unchanged. `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/artwork-store.test.ts apps/server/src/metadata/artwork-http.test.ts apps/server/src/metadata/jobs.test.ts` exit 0, 43 pass / 0 fail / 238 expects; `bun run lint`, `bun run check` and `bun run build` each exit 0 after Biome reflows and three `string | undefined` test narrowings; `git diff --check` clean.
- Codex follow-up on `bd84c8b`, uncommitted. Three real findings fixed with tests. `artwork-http.ts` gained `maxConcurrentResizes` (default 4, same `Invalid artwork cache size.` on invalid bounds) with a FIFO slot limiter acquired after route/auth/width validation but before `readArtworkOriginal`; the slot covers original reads, hashing, cache and in-flight waits, resize and response construction, hands off to waiters without decrementing, and is double-release guarded. `artwork-store.ts` records the fresh generation target/key outside the transaction; on transaction failure it best-effort queries whether any artwork row references the fresh key, deletes the unreferenced renamed file, leaves the file when verification fails, and always rethrows the original error while old selected bytes stay untouched. `readArtworkOriginal` now opens with `O_RDONLY | O_NOFOLLOW` after the parent checks, verifies `handle.stat().isFile()`, reads through the handle and closes in `finally`; an `ENOENT` open re-queries the row and retries once so a committed replacement is served instead of a transient miss, while `ELOOP`/non-file still throws `Invalid artwork storage path.` and a persistent miss returns null; an exported `ArtworkOpen` opener seam supports the test. Tests: gated resizes prove only two of three requests start work under `maxConcurrentResizes: 2` until a release lets the third proceed; invalid concurrency bounds reject; the rejection test now runs under limit 1 proving the failed resize releases its slot; the forced-rollback test now asserts the artwork directory holds only the prior generation; a new read test commits a mid-open replacement through the injected opener and gets the new generation's exact bytes and row with two opener calls; leaf-symlink rejection passes under `O_NOFOLLOW`. `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/artwork-store.test.ts apps/server/src/metadata/artwork-http.test.ts` exit 0, 39 pass / 0 fail / 208 expects; `bun run lint`, `bun run check` and `bun run build` each exit 0 after Biome reflows and a `withServer` options-type widening; `git diff --check` clean.
- Rebased onto `origin/main` `692f098`. The published branch contains repeated review ancestry, so the rebase replayed 52 commits. Conflicts kept the already merged show code and the latest #28 implementations. The obsolete `0003_nifty_mad_thinker.sql` replay was removed, leaving the tree identical to the pre-rebase result. Full gate: `bun install --frozen-lockfile` exit 0, 123 installs checked with no changes; `bun run lint` exit 0, 152 files clean; `bun run check` exit 0, 6 of 6 tasks; `bun run build` exit 0, 4 of 4 tasks; `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test` exit 0, 699 pass / 0 fail / 2998 expects across 51 files; `env -u DATABASE_URL bun test` exit 0, 419 pass / 280 skip / 0 fail / 1145 expects with the single expected database skip message.
- Pullfrog follow-up on `dc437c8`. The fixed two-attempt artwork read could return a transient 404 when two committed replacements removed both observed generations. `readArtworkOriginal` now retries only while a fresh selected-row query shows a changed storage key, and returns null when the selected key remains missing. The coordinated opener test replaces both observed generations and reads the third generation's row and exact bytes. The first parallel gate caught one Biome reflow and one `Buffer | undefined` test type; both were corrected. Final results: `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/artwork-store.test.ts` exit 0, 23 pass / 0 fail / 98 expects; `bun run lint`, `bun run check` and `bun run build` each exit 0.
- Review batch on `87fecc8`. Two Codex findings were real. TMDB success bodies now stream under an 8 MiB limit before JSON parsing, with Content-Length used only for early rejection and the existing timeout reason preserved. Declared and chunked overflow tests exercise search and fetch; invalid bounds reject before HTTP. Cached artwork results now hold one immutable Blob and reuse it across responses instead of copying the full encoded buffer per request. Existing warm-cache, concurrent-request and body assertions cover the shared response path. The CodeRabbit row-only cache lookup suggestion was rejected as advisory: `readArtworkOriginal` returns its row with bytes from the opened generation, the handler hashes those bytes before lookup, and replacement bytes cannot use the stale source key; the current cache already meets the specified one-resize behavior. The first parallel gate caught one formatter change and one BlobPart generic mismatch; both were corrected. Final results: `bun test apps/server/src/metadata/tmdb.test.ts` exit 0, 15 pass / 0 fail / 151 expects; `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test apps/server/src/metadata/artwork-http.test.ts` exit 0, 16 pass / 0 fail / 111 expects; `bun run lint`, `bun run check` and `bun run build` each exit 0.
- Before push, `origin/main` advanced to `368d6ce` with #61. The repeated published ancestry replay introduced three obsolete `readArtworkOriginal` declarations and duplicate old regression blocks, while conflict choices dropped artwork/API/job imports and wiring. Integration kept #61's change debouncer, repair, webhook and scan-option architecture; restored one latest artwork store implementation and test set; threaded folder provider ids through #61's `setItemProviderIds`; enqueued provider fetches after its per-directory movie scans; restored artwork routing and metadata job registration; and adjusted two #61 tests whose precondition or expected rows changed when folder provider ids are present. The first integrated lint/check gate exposed the duplicates and missing wiring. The first database suite had 770 pass / 8 fail, then the focused six-file retry had 101 pass / 1 fail; each failure was resolved as above. Final gate on the integrated tree: `bun install --frozen-lockfile` exit 0, 123 installs checked with no changes; `bun run lint` exit 0, 170 files clean; `bun run check` exit 0, 6 of 6 tasks; `bun run build` exit 0, 4 of 4 tasks; `DATABASE_URL=postgresql://pendia:pendia@127.0.0.1:55428/pendia bun test` exit 0, 767 pass / 0 fail / 3356 expects across 55 files; `env -u DATABASE_URL bun test` exit 0, 442 pass / 325 skip / 0 fail / 1217 expects with the expected database skip.
