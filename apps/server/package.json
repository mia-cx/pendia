{
  "name": "@pendia/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./api": {
      "types": "./src/api/router.ts"
    }
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "bun --watch src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@pendia/plugin-api": "workspace:*",
    "@orpc/client": "1.15.0",
    "@orpc/openapi": "1.15.0",
    "@orpc/server": "1.15.0",
    "drizzle-orm": "0.45.2",
    "effect": "3.22.1",
    "oauth4webapi": "3.8.7"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10"
  }
}
